Instalar frontend: Ejecutar npm install.

Instalar IA (QVAC): Asegurarse de tener el SDK de QVAC configurado según los requisitos del hackathon.

Instalar backend: Ejecutar pip install -r requirements.txt (o py -m pip install -r requirements.txt en Windows).

Arrancar el nodo: Ejecutar npm run dev.
